# ── General ──
general {
  resize_on_border = true
  layout = dwindle
}

# ── Dwindle Layout ──
dwindle {
  pseudotile = true
  preserve_split = true
  special_scale_factor = 0.8
}

# ── Master Layout ──
master {
  new_status = master
  new_on_top = 1
  mfact = 0.5
}
